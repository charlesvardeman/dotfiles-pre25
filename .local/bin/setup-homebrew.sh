#!/usr/bin/env bash


# This shell script installs homebrew command line packages
# It assumes you have installed homebrew  https://brew.sh
# And the path to the brew command has been set.
# For example: export PATH=/opt/homebrew/bin:$PATH
brew install docker
brew install jena
brew install jq
brew install curl
brew install wget
brew install universal-ctags
brew install tmux
brew install micromamba
brew install bash
brew install exa
brew install ack
brew install reattach-to-user-namespace
brew install graphviz
